{% extends "base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ parent() }}
{% endblock %}
{% block main %}
    <div id="homePage" class="ml-5">
        <h1>Bienvenido a Corpora Tools</h1>
        <div class="ml-3">
            <div class="row">
                <a class="c-sidebar-nav-link {{ app.request.get('_route') == 'misPedidos' ? 'c-active' }}" href="{{ path('misPedidos') }}">
                    <div class="card">
                        <div class="card-body">
                            <div class="text-right">
                                <span class="badge bg-secondary bg-success">New</span>
                            </div>
                            <div class="text-center">
                                <svg class="c-sidebar-nav-icon">
                                    <use xlink:href="{{ asset('build/images/free.svg#cil-transfer') }}"></use>
                                </svg>
                                <p>Gestión de Pedidos</p>
                            </div>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </div>
{% endblock %}
 {% block javascripts %}
     {{ parent() }}
 {% endblock %}